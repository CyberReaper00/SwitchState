{ config, lib, pkgs, ... }:

{
    /*
    containers.cont1 = {
	autoStart = true;
	privateNetwork = true;
	localAddress = "10.233.0.1";

	config = { config, pkgs, ... }: {
	    networking.hostName = "cont1";
	    environment.systemPackages = with pkgs; [ nvim ];
	};
    };
    */

}
